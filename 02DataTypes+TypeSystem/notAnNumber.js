// NaN (Not a Number)
// A special value representing an invalid numeric result.

// Examples:
console.log(0 / 0);           // NaN
console.log(Number("Hello")); // NaN
console.log("abc" * 2);       // NaN

// Check NaN
console.log(Number.isNaN(NaN)); // true